apiVersion: batch/v1
kind: Job
metadata:
  name: postgres-bootstrap
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: bootstrap
        image: postgres:16
        command: ["sh", "-c", "psql ..."]