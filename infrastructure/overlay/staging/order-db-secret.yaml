apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
  name: order-db-config
  namespace: platform

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: order-db-config

  dataFrom:
    - extract:
        key: /staging/order/db