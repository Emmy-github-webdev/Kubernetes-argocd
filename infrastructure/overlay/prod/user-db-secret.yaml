apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: user-db-secret
  namespace: prod

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: user-db-secret

  dataFrom:
    - extract:
        key: /prod/user/db