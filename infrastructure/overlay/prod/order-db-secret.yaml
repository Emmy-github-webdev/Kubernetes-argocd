apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: order-db-secret
  namespace: prod

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: order-db-secret

  dataFrom:
    - extract:
        key: /prod/order/db