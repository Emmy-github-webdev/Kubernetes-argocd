apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
  name: user-db-config
  namespace: platform

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: user-db-config

  dataFrom:
    - extract:
        key: /dev/user/db