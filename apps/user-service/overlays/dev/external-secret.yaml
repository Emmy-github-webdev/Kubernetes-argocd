apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: user-db-secret
  namespace: dev

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: user-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: /dev/user/db
        property: host

    - secretKey: port
      remoteRef:
        key: /dev/user/db
        property: port

    - secretKey: database
      remoteRef:
        key: /dev/user/db
        property: database

    - secretKey: username
      remoteRef:
        key: /dev/user/db
        property: username

    - secretKey: password
      remoteRef:
        key: /dev/user/db
        property: password