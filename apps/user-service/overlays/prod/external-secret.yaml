apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: user-db-secret
  namespace: prod

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: user-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: /prod/user/db
        property: host

    - secretKey: port
      remoteRef:
        key: /prod/user/db
        property: port

    - secretKey: database
      remoteRef:
        key: /prod/user/db
        property: database

    - secretKey: username
      remoteRef:
        key: /prod/user/db
        property: username

    - secretKey: password
      remoteRef:
        key: /prod/user/db
        property: password