apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: user-db-secret
  namespace: staging

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: user-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: /staging/user/db
        property: host

    - secretKey: port
      remoteRef:
        key: /staging/user/db
        property: port

    - secretKey: database
      remoteRef:
        key: /staging/user/db
        property: database

    - secretKey: username
      remoteRef:
        key: /staging/user/db
        property: username

    - secretKey: password
      remoteRef:
        key: /staging/user/db
        property: password