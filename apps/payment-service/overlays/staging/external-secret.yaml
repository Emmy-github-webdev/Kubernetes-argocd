apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: payment-db-secret
  namespace: staging

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: payment-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: /staging/payment/db
        property: host

    - secretKey: port
      remoteRef:
        key: /staging/payment/db
        property: port

    - secretKey: database
      remoteRef:
        key: /staging/payment/db
        property: database

    - secretKey: username
      remoteRef:
        key: /staging/payment/db
        property: username

    - secretKey: password
      remoteRef:
        key: /staging/payment/db
        property: password