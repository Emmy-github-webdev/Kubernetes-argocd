apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: product-db-secret
  namespace: staging

spec:
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: product-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: /staging/product/db
        property: host

    - secretKey: port
      remoteRef:
        key: /staging/product/db
        property: port

    - secretKey: database
      remoteRef:
        key: /staging/product/db
        property: database

    - secretKey: username
      remoteRef:
        key: /staging/product/db
        property: username

    - secretKey: password
      remoteRef:
        key: /staging/product/db
        property: password