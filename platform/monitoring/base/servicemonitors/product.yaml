apiVersion: monitoring.coreos.com/v1

kind: ServiceMonitor

metadata:

  name: product-service

spec:

  selector:

    matchLabels:

      app: product-service

  namespaceSelector:

    matchNames:

    - product

  endpoints:

  - port: http

    path: /actuator/prometheus

    interval: 15s