apiVersion: monitoring.coreos.com/v1

kind: ServiceMonitor

metadata:

  name: order-service

spec:

  selector:

    matchLabels:

      app: order-service

  namespaceSelector:

    matchNames:

    - order

  endpoints:

  - port: http

    path: /actuator/prometheus

    interval: 15s