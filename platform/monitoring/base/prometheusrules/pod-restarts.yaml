apiVersion: monitoring.coreos.com/v1

kind: PrometheusRule

metadata:

  name: pod-restarts

spec:

  groups:

  - name: pod

    rules:

    - alert: PodRestarting

      expr: increase(kube_pod_container_status_restarts_total[10m]) > 3

      for: 5m