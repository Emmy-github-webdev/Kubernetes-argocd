apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-admin
spec:
  data:
    - secretKey: admin-user
      remoteRef:
        key: staging-grafana/admin
        property: username

    - secretKey: admin-password
      remoteRef:
        key: staging-grafana/admin
        property: password