apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: product-postgres-exporter
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: prometheus-postgres-exporter
    targetRevision: 6.8.0

    helm:
      valuesObject:
        serviceMonitor:
          enabled: true

        config:
          datasource:
            host: product-postgres.staging.svc.cluster.local
            port: "5432"
            database: productdb

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true