apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-admin
spec:
  data:
    - secretKey: admin-user
      remoteRef:
        key: dev-grafana/admin
        property: username

    - secretKey: admin-password
      remoteRef:
        key: dev-grafana/admin
        property: password