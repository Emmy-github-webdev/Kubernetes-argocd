apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: kube-prometheus-stack

spec:
  source:
    helm:
      valueFiles:
        - $values/platform/monitoring/base/kube-prometheus-stack/values-common.yaml
        - $values/platform/monitoring/overlays/dev/values-dev.yaml