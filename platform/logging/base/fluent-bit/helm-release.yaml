apiVersion: helm.toolkit.fluxcd.io/v2

kind: HelmRelease

metadata:
  name: fluent-bit

spec:
  interval: 30m

  chart:
    spec:
      chart: fluent-bit
      version: "0.x"
      sourceRef:
        kind: HelmRepository
        name: fluent
        namespace: flux-system