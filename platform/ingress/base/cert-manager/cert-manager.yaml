source:
  repoURL: https://charts.jetstack.io
  chart: cert-manager
  targetRevision: v1.18.2

  helm:
    values: |
      crds:
        enabled: true

      replicaCount: 2

      prometheus:
        enabled: true
        servicemonitor:
          enabled: true
          labels:
            prometheus: kube-prometheus