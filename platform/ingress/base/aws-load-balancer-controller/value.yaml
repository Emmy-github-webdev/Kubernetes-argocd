clusterName: ${CLUSTER_NAME}

region: ${AWS_REGION}

vpcId: ${VPC_ID}

serviceAccount:
  create: true
  name: aws-load-balancer-controller

replicaCount: 2

metrics:
  serviceMonitor:
    enabled: true
    additionalLabels:
      prometheus: kube-prometheus