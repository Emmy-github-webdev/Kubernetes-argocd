apiVersion: velero.io/v1
kind: Schedule

metadata:
  name: daily-backup
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"

spec:
  schedule: "0 2 * * *"

  template:
    ttl: 168h
    includedNamespaces:
      - default