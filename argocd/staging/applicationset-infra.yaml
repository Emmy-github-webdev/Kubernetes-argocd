apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet

metadata:
  name: staging-infrastructure
  namespace: argocd

spec:
  generators:
    - list:
        elements:
          - name: ingress
            path: infrastructure/overlay/staging
            namespace: staging

  template:
    metadata:
      name: '{{name}}-staging'

    spec:
      project: default

      source:
        repoURL: https://github.com/Emmy-github-webdev/Kubernetes-argocd.git
        targetRevision: main
        path: '{{path}}'

      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true                                                                                                                                                                             